node_modules/
uploads/
output/

# GCP service account keys
*-key.json
chatterforms-*-key.json

# Environment variables
.env
.env.local
.env.production
test-secrets.js

# MaxMind GeoIP database directories (but include the .mmdb file itself)
GeoLite2-City_*/
GeoLite2-City.tar.gz
test-phase2-config-local.sh

# Planning and review documents (not tracked)
plan/
